// =============================================================================
// 🎣 PLOTTING HOOKS - MODULAR VERSION
// =============================================================================

// Re-export original hooks for backward compatibility
export {
  usePlotConfig,
  useInteractionConfig,
  usePlotEvents,
} from "../usePlotterHooks";
